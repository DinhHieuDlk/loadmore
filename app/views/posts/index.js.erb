$('.posts').append('<%= j render @posts %>');
<% if @posts.current_page == @posts.total_pages %>
$('.loadmore').remove();
<%else%>
$('.loadmore').replaceWith('<button><%= j link_to "Load More Post;",posts_path(:page => @posts.next_page), :remote=>true%></button>');
  <%end%>         